# Session D — Review & land

_Loaded by Session D. Assumes the spine's §Invariants + §Thinking budget + §Swap (`.orchestrator/orchestrator.md`) and `~/.claude/orchestrating/safeguards.md`._

C's notes are **claims, not gospel** — verify load-bearing ones against committed code.

1. **Completeness** — walk `spec.md` epics + acceptance criteria against the merged trunk (trunk = `integration` pre-go). Anything unmet → list it, then classify by blast radius: **gap in a single unit's files-owned → mini-brief a unit-builder + re-verify** (dispatch carries `upstream: <relevant .done.md paths|none>` — the def's GATE hard-fails without the line; D never builds in its own window — reviewer stays reviewer); **gap touching >1 unit's files-owned (or a cross-unit regression — failure spanning units, not one unit's gap) → hand back to C**, never a single unit-builder. Hand-back: the gap units are absent from prd.json/depmap/briefs/, so D first writes each mini-brief to `briefs/<gap-unit>.md` + appends its row to `depmap.md`, `prd.json` (gap rows carry `agent: unit-builder` unless a specialist clearly fits), and `progress.md` (`<unit> | ⏳ | —` — a crashed resume reconstructs from progress.md and would else silently drop them), then fills the FULL `handoff.md` schema (`done:` = all merged units — the resume merge-check greps it; `next wave: <gap units>`), prints the C **resume** kickoff for a fresh chat, and stops — the fix wave runs the integration gate, then a fresh D pass reviews the delta only. Small enough to flag → flag.
2. **Full code review** — across all units (`code-review`/`pr-review-toolkit`): branch diff only, diff via `gh`/git not MCP, isolated subagents returning confidence-gated findings **(≤5 per unit, one line each — an uncapped findings list re-floods D exactly like a verbose return)**; small model for style, large for logic. **Judge = ONE producer-blind rubric-scored call per unit** (via the `blind-judge` def; dispatch = the unit's spec criteria + branch/diff ref ONLY, never `.done.md` bodies or producer notes — D holds every `.done.md`, so this is the leak point) (accuracy / completeness / spec-fit — single call matches human judgment best; a single judge misses ~1 in 5 real issues, acceptable for routine units); escalate to a **2–3 heterogeneous-lens panel** for load-bearing or contested units. Deterministic tests stay the gate for exact correctness — a judge verdict never overrides a red test.
3. **Land** — `main` fast-forwards to C's already-regression-green `integration` branch (no re-merge). **`ff-only` fails (main diverged since the run began)** → merge `origin/main` INTO `integration`, dispatch the integration agent for a regression pass on the result, then ff; **never rebase `integration`** (rewriting it orphans the unit branches). Irreversible + outward-facing: **show the user the plan and get an explicit go first** (what merges, what pushes, what gets deleted). **User says NO** → record the objection, then fork: *fix* (objection is actionable → mini-brief unit-builder wave, re-verify, re-ask) · *park* (write `handoff.md` with `next: D land-gate re-ask — objection: <x>` + a D-resume kickoff line [fresh chat reads session-d.md + handoff.md + progress.md, re-runs step 3 from the objection; handoff.md missing → recover the objection from the `state.md` line; `git rev-parse integration` ≠ the last SHA recorded in progress.md → integration was touched while parked → dispatch the integration agent for a regression pass before re-asking the land gate] FIRST, then write `state.md` = `state: parked — <objection>` — marker = commit point, so this order survives a crash between the two; keep all branches and `.orchestrator/`, skip cleaning — cleaning's not-landed gate recognizes `state: parked` and offers this D-resume) · *abandon* (manifest the unit branches for deletion, get a second explicit confirm — deleting unmerged work — then D EXECUTES the deletions, `git branch -D` each listed branch — else stale branches collide with future wave-1 `checkout -b`; write `state.md` = `state: abandoned — <reason>` — one line — so a later cleaning run treats the state as decided instead of re-asking, per cleaning lite's gate; leave `.done.md` harvest to cleaning's abandoned path — D doesn't harvest here, else duplicate lines in the same decisions file). On go: `merge --ff-only integration` into `main` → `push -q` → write `state.md` = `state: landed — <YYYY-MM-DD>`. **Prune only the merged `<prefix>/<unit>` + `integration` branches** (`git branch -d` — safe-delete refuses unmerged; never `-D` blanket, never touch branches outside this run). Blocked/unmerged units stay. One clean trunk.
4. **Report** (caveman-terse, one line/unit):
   ```
   blockers: <unit: reason>     branches→PRs: <unit → PR>     savings: rtk gain
   ```
   **Partial-release policy** (decided in A, per unit): blocked unit → *ship-without* (drop + log) or *block-release* (hold version).
5. **Catalog update** — the agents catalog (`~/.claude/agents-docs/`) is living; this cycle feeds it: agent type/role used that has no row → add its row to the right category file; an agent surprised (wrong tier, bad returns, cost blowout, unexpectedly great fit) → one dated line under that category's `## Lessons`; catalog vs actually-available agent types drifted (plugin renamed/removed) → flag in the report. Rows stay one line; restructuring is cleaning's job, not D's. **Catalog edits dirty the symlinked DOTFILES repo — stage only the catalog paths (`git add` the agents-docs dir, never `-a` — dotfiles may hold unrelated WIP), commit them there (`chore:`, `-q`) before ending.**
6. **Clean** — end by printing the cleaning kickoff for a fresh chat: `Read ~/.claude/cleaning/README.md and run it.` (harvests durable knowledge from `.orchestrator/` into docs/ — see cleaning's harvest table — THEN resets spent state, merged branches, junk; refreshes docs + the agents catalog). **Don't pre-delete `.orchestrator/` — cleaning consumes the `.done.md` files.**
