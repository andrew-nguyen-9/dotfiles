# Session D — Review & land

_Loaded by Session D. Assumes the spine's §Invariants + §Thinking budget + §Swap (`.orchestrator/orchestrator.md`) and `~/.claude/orchestrating/safeguards.md`._

C's notes are **claims, not gospel** — verify load-bearing ones against committed code.

1. **Completeness** — walk `spec.md` epics + acceptance criteria against the merged trunk (trunk = `integration` pre-go). Anything unmet → list it, then classify by blast radius: **gap in a single unit's files-owned → mini-brief a unit-builder + re-verify** (dispatch carries `upstream: <relevant .done.md paths|none>` — the def's GATE hard-fails without the line; D never builds in its own window — reviewer stays reviewer); **gap touching >1 unit's files-owned (or a cross-unit regression — failure spanning units, not one unit's gap) → hand back to C**, never a single unit-builder. Hand-back: the gap units are absent from prd.json/depmap/briefs/, so D first writes each mini-brief to `briefs/<gap-unit>.md` + appends its row to `depmap.md`, `prd.json` (gap rows carry `agent: unit-builder` unless a specialist clearly fits — but if these gap rows hold agent-def types while `progress.md`'s header says `driver: ralph`, flip the header to `driver: manual`: ralph composes its own prompts and never injects the defs' `upstream:`/`notes:` contract lines, so every gap unit would GATE-fail in a loop), and `progress.md` (`<unit> | ⏳ | —` — a crashed resume reconstructs from progress.md and would else silently drop them), then fills the FULL `handoff.md` schema (`done:` = all merged units — the resume merge-check greps it; `next wave: <gap units>`; `window resets: n/a — hand-back` is the legal value, D isn't budget-stopped), prints the C **resume** kickoff for a fresh chat, and stops — the fix wave runs the integration gate, then a fresh D pass reviews the delta only. A unit already ⛔ in `progress.md` (C exhausted retries + release policy applied) is NOT a "gap" — route it to step 4's partial-release report, never mini-brief/hand-back (re-briefing a 3×-failed unit = a 5th blind attempt). Small enough to flag → flag.
2. **Full code review** — across all units (`code-review`/`pr-review-toolkit`): branch diff only, diff via `gh`/git not MCP, isolated subagents returning confidence-gated findings **(≤5 per unit, one line each — an uncapped findings list re-floods D exactly like a verbose return)**; small model for style, large for logic. **Before dispatching any judge, put D's working checkout on `integration`** (`git checkout integration`) — the judge runs the acceptance checks against the already-checked-out tree and never checks out itself; a checkout still on `main` (the normal pre-ff state) runs every check against code WITHOUT the units' work → false `unmet` → needless panel escalation. **Judge = ONE producer-blind rubric-scored call per unit** (via the `blind-judge` def; dispatch = **the unit's spec criteria — read the unit's `briefs/<unit>.md` objective (it carries the criteria fanned from spec.md; for a module-named unit that doesn't map 1:1 to a spec epic, the brief is the only unit→criteria link)** + branch/diff ref ONLY, never `.done.md` bodies or producer notes — D holds every `.done.md`, so this is the leak point; pin the diff base to `$(git merge-base integration <branch>)..<branch>` — a wave-N branch cut off an already-updated integration otherwise shows all prior waves' work as this unit's diff) (accuracy / completeness / spec-fit — single call matches human judgment best; a single judge misses ~1 in 5 real issues, acceptable for routine units); escalate to a **2–3 heterogeneous-lens panel** for load-bearing (a foundation unit or one with ≥2 depmap dependents) or contested (any judge score ≤2, or `unmet` non-empty) units. Deterministic tests stay the gate for exact correctness — a judge verdict never overrides a red test.
3. **Land** — `git fetch` first (remote may have diverged unseen — else a local ff succeeds then `push -q` non-ff-rejects, unhandled), then `main` fast-forwards to C's already-regression-green `integration` branch (no re-merge). **`ff-only` fails (main diverged since the run began)** → merge `origin/main` AND local `main` INTO `integration` (local `main` carries A's CLAUDE.md patch + run-local commits origin lacks — merging origin alone leaves ff failing forever), dispatch the integration agent for a regression pass on the result, then ff; **never rebase `integration`** (rewriting it orphans the unit branches). Irreversible + outward-facing: **show the user the plan and get an explicit go first** (what merges, what pushes, what gets deleted). **User says NO** → record the objection, then fork: *fix* (objection is actionable → mini-brief unit-builder wave, re-verify, re-ask) · *park* (write `handoff.md` with `next: D land-gate re-ask — objection: <x>` + a D-resume kickoff line [fresh chat reads session-d.md + handoff.md + progress.md, re-runs step 3 from the objection; handoff.md missing → recover the objection from the `state.md` line; `git rev-parse integration` ≠ the last SHA recorded in progress.md (D writes this SHA into progress.md as part of the park write below — guarantees the comparand exists even when C crashed before recording one) → integration was touched while parked → dispatch the integration agent for a regression pass before re-asking the land gate] FIRST, then record `git rev-parse integration` into progress.md (the resume comparand above), then write `state.md` = `state: parked — <objection>` — marker = commit point, so this order survives a crash between the two; keep all branches and `.orchestrator/`, skip cleaning — cleaning's not-landed gate recognizes `state: parked` and offers this D-resume) · *abandon* (manifest the unit branches **+ `integration` itself** (holds only abandoned wave merges — same second-confirm covers it; a surviving abandoned `integration` else makes every future cycle's A hit "unmerged → STOP and ask" forever) for deletion, get a second explicit confirm — deleting unmerged work — then D EXECUTES the deletions, `git branch -D` each listed branch — else stale branches collide with future wave-1 `checkout -b`; user DENIES the second confirm → skip all deletions, keep the branches (report the list only), but STILL write `state.md` = `state: abandoned — <reason>` (the decision is made even though the branches survive — without the marker cleaning's gate + every future session warn "possibly live" forever); write `state.md` = `state: abandoned — <reason>` — one line — so a later cleaning run treats the state as decided instead of re-asking, per cleaning lite's gate; leave `.done.md` harvest to cleaning's abandoned path — D doesn't harvest here, else duplicate lines in the same decisions file). On go: `merge --ff-only integration` into `main` → `push -q` → write `state.md` = `state: landed — <YYYY-MM-DD>` **only after push exits 0** (push fails on network → report it, leave `state.md` at `running`, keep everything — cleaning's gate + a re-run of step 3 recover; push rejected by BRANCH PROTECTION → never force, open a PR `integration`→`main`, report; the PR merges (you observe it) → `git fetch` + reset local `main` to `origin/main`, then delete `integration` + the merged unit branches with `git branch -D` (a squash/rebase PR merge lands the content but rewrites ancestry, so `-d`'s safe-delete + `merge-base --is-ancestor` + next-cycle A's "unmerged → STOP" all false-fail forever — `-D` is correct here precisely because the ancestry check would lie), THEN write `landed`; merge handed to the user → leave `state.md` at `state: running` and write `handoff.md` with `next: PR pending — on merge: write landed + -D integration/unit branches` (D can't observe the merge, so a fresh session finishes the land)). **Prune only the merged `<prefix>/<unit>` + `integration` branches** (`git branch -d` — safe-delete refuses unmerged; never `-D` blanket, never touch branches outside this run; a listed branch already gone — the user deleted it — → skip it, don't error the loop). Blocked/unmerged units stay. One clean trunk.
4. **Report** (caveman-terse, one line/unit):
   ```
   blockers: <unit: reason>     branches→PRs: <unit → PR>     savings: rtk gain
   ```
   (`rtk gain` fails or is missing → write `savings: n/a`.) **Partial-release policy** (decided in A, per unit): blocked unit → *ship-without* (drop + log) or *block-release* (hold version). **Design North Star flag:** repo has `design/INDEX.md` whose `last-challenged:` is overdue per its §Cadence (`each release` → always flag at a land) → add one line to the report (fix = a `~/.claude/design-system-creator/` challenge run, not a D task) — D is a land step, and `challenge.md` claims land steps flag overdue cadence.
5. **Catalog update** — the agents catalog (`~/.claude/agents-docs/`) is living; this cycle feeds it: agent type/role used that has no row → add its row to the right category file; an agent surprised (wrong tier, bad returns, cost blowout, unexpectedly great fit) → one dated line under that category's `## Lessons`; catalog vs actually-available agent types drifted (plugin renamed/removed) → flag in the report. Rows stay one line; restructuring is cleaning's job, not D's. **Catalog edits dirty the symlinked DOTFILES repo — stage only the catalog paths (`git add` the agents-docs dir, never `-a` — dotfiles may hold unrelated WIP; first `git status --porcelain` the dir — pre-existing dirty paths D didn't touch → `git add` ONLY the files D edited, else a blanket dir-add sweeps the user's half-edits into D's chore commit), commit them there (`chore:`, `-q`) before ending.**
6. **Clean** — end by printing the cleaning kickoff for a fresh chat: `Read ~/.claude/cleaning/README.md and run it.` (harvests durable knowledge from `.orchestrator/` into docs/ — see cleaning's harvest table — THEN resets spent state, merged branches, junk; refreshes docs + the agents catalog). **Don't pre-delete `.orchestrator/` — cleaning consumes the `.done.md` files.**
