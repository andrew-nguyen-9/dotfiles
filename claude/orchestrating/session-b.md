# Session B — Architecture

_Loaded by Session B (fresh chat). Assumes you've already read the spine's §Invariants + §Thinking budget + §Tiers and the project copy's §Swap values (`.orchestrator/orchestrator.md`)._

All design lives here so C stays empty. Read `spec.md` ONCE → artifacts; never re-read it. **spec.md >600 lines** → the orch read-cap hook blocks a whole-file Read; read it in offset/limit windows (still ONCE, just chunked).

- **Plan & split** — `superpowers:writing-plans` → units (epic/feature/module/endpoint per project). `ralph-skills:prd` → `ralph` → `prd.json` (machine task list).
  - **Minimal schema C reads:** each task `{id, brief (path), deps [ids], effort, agent}`. Ralph's output is a superset — fine as long as those fields exist. **After generating `prd.json`, B greps every task for all 5 fields** — any missing → fix before kickoff. **`prd.json` `deps` disagree with `depmap.md` edges → `depmap.md` wins** (patch `prd.json`).
  - **`agent` (required)** — B picks each unit's type from the catalog: `~/.claude/agents-docs/README.md` task-verb tree → load **only** the matching category file. No specialist fits → `general-purpose` (a good brief beats a mis-fit specialist). **B verifies availability at pick time** (plugin rows: is the plugin enabled in THIS repo? — the check reads the ACTUAL `~/.claude/settings.json` `enabledPlugins` + the target repo's `.claude/settings.json` `enabledPlugins` — `PLUGINS.md` is documentation, drifts, never the sole source) — an unknown type doesn't error at dispatch, the harness silently runs general-purpose, so B is the only real checkpoint. Plugin missing → the category's Fallback chain, and **bake the fallback's brief-amendment into the brief itself** (e.g. "≤2 files, diff-edit only") so C never needs the catalog. **Record every fallback/substitution in `progress.md`** — C dispatches verbatim and can't detect the harness's silent general-purpose swap; B is the only writer that knows (and D reads it).
  - **Requires** the `ralph-loop` + `ralph-skills` + `superpowers` plugins (C drives `ralph-loop` on `prd.json`). Check at the start of B — missing → **fallback**: hand-write `prd.json` to the schema above; C dispatches units itself in dep order with `dispatching-parallel-agents` (no ralph-loop; superpowers also missing → C dispatches via the Agent tool in dep order, bare). Same artifacts, manual driver. **Persist the choice** — B's chat dies with B: `progress.md` header line `driver: ralph|manual`; C runs whichever it says. **ralph AND superpowers both missing** → write `driver: manual — no superpowers` (C then dispatches via the bare Agent tool, skipping superpowers skills). **`prd.json` contains agent-def types (unit-builder/integration-agent/blind-judge) → `driver: manual` MANDATORY** — ralph-loop composes its own task prompts and never injects the dispatch-contract lines (`upstream:`/`notes:`/`base:`/`isolation`) the defs' GATEs hard-fail without; ralph driver only for pure non-def fan-outs.
- **Doc placement** — B's design docs/trackers land by lifespan: **durable design knowledge → `docs/design/` or `docs/research/`** (committed, survives cleaning); **cycle-scoped trackers → `.orchestrator/`** (dies at cleaning). Default-unspecified → durable docs wrongly land in `.orchestrator/` and get deleted. **Durable design docs commit on `integration`** — wave-1 units branch off it and see them; committing on `main` after `integration` branched diverges the trunk (every unit branches off `integration`) and breaks D's final ff.
- **Briefs** — `briefs/<unit>.md`, standalone. *Standalone test:* an agent given only its brief can finish. Needs the spec → brief is incomplete; fix it. **Atomic unit over the brief cap** (Invariant 3, can't split) → overflow detail to a sidecar `briefs/<unit>-spec.md` path-ref'd from the brief (path-ref'd files count as standalone); split only when genuinely divisible. **Required skeleton — delegation failures come from missing fields, not length:** `objective / boundaries (files OWNED, exclusions) / tool guidance / return format [/ dod: <commands> when the unit's gate differs from CLAUDE.md — monorepo package, research/docs-only unit]`. **Agent-def types (unit-builder, integration-agent, blind-judge) get NO role snippet** — the contract is baked into the def; pasting it again duplicates ~300 tok/dispatch. Other types: compose the ≤5-line role snippet from the row's Use-when + Returns columns (+ the Fallback line if applying one) — category files carry no snippet blocks (B composes at write time; C never opens the catalog). Research/docs-only units: `dod:` = the smallest runnable check that fails if the work is wrong (assert script, links resolve, claims match code) — never a fake build-green. Repo/stack context = **path-ref the needed `02` SECTIONS (§Map / §Conventions / §Gotchas)**, not the whole file (or the repo map), never re-described per brief. **Smallest high-signal set** — a fat brief is *less* accurate (irrelevant context worsens hallucination) and costlier. **High-fan-out briefs → ultrathink** (spine §Thinking budget); leaf briefs, normal effort. Effort field rubric: est. tool calls — ≤10 → haiku-ok, 10–15 → default, more or design-heavy → hard (or split the unit). **Greenfield: any wave-1 unit WITHOUT a foundation dep MUST carry a brief `dod:`** (or gain the dep) — the repo CLAUDE.md DoD commands aren't real until foundation lands, so such a unit otherwise can't go green. **Monorepo: per-package DoD lives in `docs/02-architecture.md` §Test layout** (per-package table) when present — brief `dod:` copies from it; CLAUDE.md DoD stays repo-level.
- **Dependency map** — the one genuinely project-specific artifact; write it to `depmap.md` (the table below), since C reads it cold. **Check acyclicity before writing `prd.json`** — a dependency cycle (A→B→A) has no valid wave order; **first delete any edge you added on "unsure"** (below) — only a real cycle survives — then collapse the survivor into ONE sequential unit (the over-decomposition rule — session-c.md §Run). Wrong edge → dependent runs blind. Unsure → **add the edge** (extra path is cheap; stale build is not). **`files-owned` column is mandatory** — **seed it from `docs/02-architecture.md` §Ownership zones when present** (zone lines lead with the glob); don't rebuild from scratch what harvest recorded. Two agents editing one file is the most reliable way to corrupt output; two units claiming the same path can't share a wave (B merges them into one unit, or C's pre-dispatch scan catches it: **before each wave C checks the wave's files-owned for overlap — expand each glob against `git ls-tree -r --name-only integration` (prior waves' files exist only on `integration`, not the main checkout) and compare file sets; a unit whose glob expands EMPTY (files it will CREATE) → compare that unit by directory prefix instead (brownfield new-file units are otherwise invisible to the scan); greenfield (files don't exist yet) → compare directory prefixes. Overlap → defer the dep-later unit one wave; overlapping pair with no dep relation → defer the unit with fewer downstream dependents**). Discovered mid-wave anyway → treat as an integration conflict: C names one unit the owner, the other's overlapping hunks re-do on top after merge. Fan out *reads* freely; fan out *writes* only with disjoint ownership. **When writing `depmap.md`, reconcile `blockers.md` `blocks:` fields to the FINAL unit ids** — A wrote them before units existed (guesses); patch blockers.md. **A box whose epic was scoped out** (maps to NO final unit) → rewrite `blocks: none — scoped out`; never leave a stale `all`.

  | Unit | Upstream `.done.md` to inject | files-owned |
  |------|------------------------------|-------------|
  | auth-mw | — | `src/middleware/*` |
  | user-api | `auth-mw.done.md` | `src/api/users*` |
  | billing | `auth-mw.done.md`, `user-api.done.md` | `src/billing/*` |

- **Tracker** — seed `progress.md`: header lines (`driver: ralph|manual`, `guard: active|INACTIVE — <why>` — seed via `command -v ccusage`; C's pre-flight overwrites this seed with ground truth) + a `unit | status | branch` table (statuses ⏳→🔁→✅/⛔). C updates it as waves land; it's the at-a-glance state for resume + the Block-8 report — **and the crash-recovery source when no handoff.md exists**.
- **Foundation-first** — epics share scaffolding (schema, shared types, design system, auth, config). Make it **unit #1** (map: all → foundation), run as the first wave; its `.done.md` carries shared decisions. Genuinely independent units skip it — don't invent a foundation. **Brownfield: existing shared code already IS the foundation** — point briefs at it (paths/symbols), don't rebuild; only add a foundation unit for *new* shared scaffolding. **Big-tier greenfield → the foundation brief includes "seed `docs/02-architecture.md` headings per `~/.claude/cleaning/structure.md` §02".**
- **`.done.md` contract** — every C agent writes this on finish (≤15 lines, caveman-compressed, disk only, pass the **path** not the body):
  ```
  # <unit>.done.md  (≤15 lines)
  shipped: <what>     files: <paths>
  verified: <re-runnable/idempotent cmd/test that proves each shipped claim>
  decided: <non-obvious choices>
  gotchas: <traps for dependents>
  branch: <branch> / PR: <null|url>
  ```
  A header with genuinely nothing to report → write the literal `none` (C's gate requires non-empty bodies; a truthful-empty header otherwise costs a SendMessage round). A claim without a `verified:` line is exactly what D must re-check; dependents verify upstream symbols exist (Serena) before building on them. **`verified:` cmds must be re-runnable/idempotent** — the integration agent's wave-1 audit and Session D re-execute them, so a mutating probe (`db:migrate`, seed) corrupts shared state on re-run.

**B → C kickoff (copy-paste):**
```
Session C — Execution. You are the orchestrator: hold ONLY the dependency map + short statuses.
Activate /caveman:caveman ultra + /ponytail:ponytail ultra + RTK + Serena. Output styles off.
Read ~/.claude/orchestrating/session-c.md + ~/.claude/orchestrating/efficiency.md (dispatch skeleton) + ~/.claude/orchestrating/safeguards.md §Loop safeguards (your live 3-kill procedure) + .orchestrator/orchestrator.md §Swap + §Model + §Invariants + §Thinking budget + .orchestrator/depmap.md. Work off depmap.md + prd.json + briefs only — do NOT open spec.md (detail is the agents' job).
GATE 1: the repo CLAUDE.md is the single DoD source — it must hold REAL values on its DoD / Secrets / Branches lines; any literal `<...>` token ON the canonical DoD / Secrets / Branches / Map / Ops-env lines (or a missing line) → STOP — a filled line elsewhere legitimately containing angle brackets (`Test-one: pytest <pattern>`, JSX in conventions) must NOT false-STOP; finish Session A's blank-fill before any dispatch. On a full-docs-tier repo also `test -f` the `Map:`/`Ops/env:` pointer targets — a dangling pointer → fix before dispatch. (The dispatch skeleton says "DoD per repo CLAUDE.md" — it never duplicates commands.)
GATE 2: read .orchestrator/blockers.md — unchecked boxes block ONLY the waves that depend on them (blocks: field); a wave with a blocked dep does not dispatch. All of wave 1 blocked → STOP and list. A blocker box's "how to verify" that looks mutating (write/delete/POST) → don't run it; route to the user. `verify: user-attested` → trust the tick, no probe.
Then pre-flight budget, run waves off prd.json. Inject brief path + upstream .done.md paths + `notes: .orchestrator/<unit>.done.md` into each dispatch — nothing else. End by printing the Session D kickoff prompt.
```
