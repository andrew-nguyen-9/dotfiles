# Roadmap

- OPTIONAL (cost only, not gating): uninstall reinstalled output-style plugins so agent windows stay thin — `claude plugin uninstall explanatory-output-style` + `learning-output-style` (names per `claude plugin list`).
