# Integrate and land agents

Use `integration-agent` to review disjoint unit outputs, identify conflicts, and run the combined DoD. Because subagents share the workspace, the root agent performs branch changes, commits, merges, pushes, and PR creation. Landing always requires the authority stated by the user.
