# Documentation review agents

Use `default` or `explorer` read-only to verify claims against code, check links and paths, and find missing operational guidance. Use the repository's document validator as the final gate.
