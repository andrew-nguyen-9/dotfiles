# Plan agents

Use the root agent for final scope and ownership decisions. A `default` agent may independently challenge a decomposition or inspect one bounded subsystem when delegation is authorized. Planning agents do not edit files unless their brief explicitly owns a planning artifact.
