# Codex setup agents

Use the official OpenAI documentation skill for current Codex configuration, `AGENTS.md`, hooks, skills, plugins, MCP, or custom-agent behavior. Prefer official docs and verified local state over remembered product behavior.
