# Security agents

Use a security-focused review skill or a read-only `blind-judge` for auth, secrets, input validation, permissions, or trust-boundary changes. Findings need concrete path/line evidence and a feasible fix. Never load secret values into context.
