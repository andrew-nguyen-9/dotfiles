# Design and UI agents

Use the installed design consultation/review skills when applicable. Delegate implementation to `unit-builder` only with explicit component/file ownership. Load the project's design North Star first; fall back to `~/.codex/design-guidelines/INDEX.md` for gaps.
