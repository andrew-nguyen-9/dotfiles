# Wishlist router

This file is a template. Copy it into the target repository; never fill the symlinked copy in place.

## Wishlist

- Outcome:
- In scope:
- Out of scope:
- Constraints:
- Verification / DoD:
- Branch prefix:
- Delegation allowed: yes / no

## Routing instructions

If the Wishlist is blank, stop and ask the user to fill it.

- One focused outcome, one or two files, no coordination: read `~/.codex/orchestrating/lite.md` and run it.
- Several bounded units that fit one task: read `~/.codex/orchestrating/medium.md` and run it.
- Multi-wave, multi-day, or durable handoff required: read `~/.codex/orchestrating/orchestrator.md` and run Session A.

When uncertain, start smaller. Escalate only after writing the current state and preserving a green checkpoint.
