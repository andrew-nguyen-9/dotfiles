# Programmatic dispatch notes

When a Codex surface exposes programmatic multi-agent controls, preserve the same contract as `efficiency.md`:

- stable task identifier;
- explicit owned files;
- dependency-ready input paths;
- bounded runtime and concise output;
- parent-owned integration and final verification.

Do not assume a particular tool name or private API. Use only capabilities exposed in the active Codex surface.
